# TypeScript Coding Standard

- Do not use the `any` type under any circumstances.
- Use specific interfaces, types, or `unknown` if the type is truly dynamic.
- Always prefer strict typing to ensure type safety.
